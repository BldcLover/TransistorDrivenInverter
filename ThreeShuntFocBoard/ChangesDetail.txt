Commit 2:  
        - Changed INA240 power supply to 3.3VDC instead of +5VDC, as the MCU operates at 3.3VDC.  
        - Replaced R47 with a 500mA fuse for safety.  
          *(Initially, I hadn't decided what component to place there, so I used R47 as a placeholder.)*  
        - Designed additional phases and updated PWM pin naming inside each PhaseX schematic (e.g., renamed PB_PWM to PB_HIGH).  
        - Renamed Phase A schematic:  
          - Previously, the sheet name was Phase A, but the sheet file was named inverter.  
          - Deleted the old inverter file and created a new PhaseA file instead.
          - change hall sensor circuit remove GND 
Commit 3:  
	- I have used the STM32G431C6Tx [LQFP-48] MCU and assigned all pins as per the Excel file shared in the same folder.  
	- Peripheral connections are not yet designed as per the selection—this is still a work in progress. For now, I have only set the pins and their functionalities.  
	- I am planning to read only Phase A and Phase B current and voltage ADCs, as the same ADC group is not available for all three phases.  
	- Therefore, I have removed the Phase C current sensing.
	
	Note: Here I have used ADC1 Group for phase current, voltage and battery sense.

Commit 4:  
	- Added load capacitors to crystal connections.  
	- Assigned unused GPIOs as test points.  
	- Connected PB3 (SWO) to the programming connector for debugging.  
	- Set boot pin PB8 to weak pull-up and repurposed it as CAN_RX.  
	- Added Pi filter before VDDA pin for improved supply filtering.  
	- Noted used peripherals (PWM and ADCs only) in MCU section.  

Commit 5:
	- Adjusted the VDD pin connections and added decoupling capacitors.
	- Provided a filtered supply (VDDA) to the VREF pin for improved ADC accuracy.
	- Added a 220Ω resistor at the PF1 pin, based on the B-G431B-ESC1 schematic.
	- Modified INA240 VS and REF2 connections, now supplying filtered VDDA for better output accuracy.
	- Change current sense shunt value to 1milli ohm.


